import com.utils.SimpleVerifier
import internal.GlobalVariable

// Set global variables before running (or pass them dynamically if needed)
GlobalVariable.calcId = 'auto05'
GlobalVariable.category = 'auto'

SimpleVerifier.verifyCalculator()
